# 小手机领域词典

小手机是在 SillyTavern 中运行的一组系统 App。这里统一记录产品讨论、规格和代码中必须保持一致的领域术语。

## 小手机外壳

**全局应用状态栏**:
由小手机外壳统一渲染的唯一一套时间、信号、Wi-Fi 和电池区域。非首页 App 使用全局主题模式对应的前景色；首页的整套状态栏改用“首页 App 名称颜色”，时间与三个设备图标必须保持同色。各 App 页面只为它保留顶部安全区，不在 App 内复制第二套状态栏。
_避免：每个 App 自建状态栏、重复时间和设备图标、App 内容占用顶部安全区_

**Home Indicator**:
除小手机主页面外，所有 App 页面底部共同显示的横线形全局返回元素。点击或轻触会返回小手机主页面，不会关闭小手机；主页面本身不显示，因为用户已经位于目标页面。
_避免：只在 QQ 中显示、主页面重复显示、纯装饰横线、点击后关闭小手机_

**外壳命名空间**:
小手机 shell 的 DOM class、CSS 选择器和自定义变量必须使用 Yuzi 独占命名空间。屏幕与 Home Indicator 分别使用 `.yuzi-phone-screen`、`.yuzi-phone-home-indicator`；不得暴露 `.phone-screen`、`.phone-home-indicator` 等通用 shell class，也不得新增无 `--yuzi-phone-` 前缀的自定义变量，以免其他手机扩展的全局样式改变布局或隐藏控制。
_避免：依赖加载顺序覆盖冲突、用 `!important` 与其他扩展对抗、通用 shell class、无 Yuzi 前缀的 CSS 变量_

**全局主题模式**:
只在小手机“设置 → 界面外观”中选择的白天或夜间外观模式；各 App 消费同一模式，不建立 App 内主题开关。QQ 白天模式一一对应 Figma 的 QQ 浅色皮肤，夜间模式一一对应其旁边的 QQ 深色皮肤。
_避免：QQ 自建主题入口、QQ 与小手机主题不同步、运行时混用两套皮肤_

**共享图片裁剪层**:
小手机中仍需要裁剪的上传场景共用的图片选择后裁剪界面。它始终限制在小手机壳内部，使用与 QQ 删除好友弹窗一致的视觉骨架，不遮盖壳外的 SillyTavern 页面；QQ 头像、资料背景、聊天背景和表情明确不消费该层，而是保存原始图片 Blob。
_避免：覆盖整个浏览器、不同 App 各造裁剪弹窗、QQ 上传被隐式裁剪或压缩、同一手机内叠加多层遮罩_

## QQ

**QQ 系统 App**:
独立于表格和小剧场的即时聊天系统，拥有自己的会话、消息、媒体和运行规则。
_避免：消息记录表、QQ 表格、消息小剧场_

**会话**:
一个私聊或群聊的完整聊天单元，包含参与者、消息、资料和自己的世界书注入状态。会话内消息使用统一选择模式删除，可单选、多选或全选。
_避免：消息表、聊天表_

**聊天输入区**:
私聊页底部持续可用的消息发送区域。纯文字输入不显示发送按钮，输入框最多展开四行；桌面键盘 Enter 发送、Shift+Enter 换行，移动端键盘发送键提交。单条用户文字不设产品级长度上限，也不显示字数计数器。第二行工具栏中麦克风、图片、相机、钱包和表情分别创建叙事语音、叙事图片、叙事视频、转账和表情消息；前三者输入的是文字内容或自然语言描述，不调用真实麦克风、相册、摄像头或媒体文件。语音、图片、视频和转账在确认后、表情在点击后，立即作为独立消息发送，走正常回复排队；它们不读取、不清空、不组合主文本草稿。加号当前只保留视觉，不打开面板或执行动作。工具栏在基准宽度下沿用 Figma 间距；小手机缩窄、六个图标无法完整容纳时保持单行并允许横向滚动，不换行、不隐藏入口。QQ 自有内容弹层统一使用删除好友弹窗的视觉骨架；打开后自动聚焦，空内容禁用确认，不设产品级字数上限，Enter 确认、Shift+Enter 换行，提交失败保留弹层和原文。QQ AI 生成中仍允许继续发送，由未回复批次统一承接后续消息；非好友私聊替换为只读状态，未来恢复群聊后禁言、已退出或已解散群聊也沿用只读规则。
_避免：可见文字发送按钮、把输入框四行高度当成消息长度上限、生成时锁住输入、让工具消息读取、清空或组合主文本草稿、把叙事媒体误接为真实设备或文件、把加号接入未定义功能、将五个消息入口降级为装饰、只读会话仍可发言_

**聊天输入性能边界**:
聊天输入框的自动高度测量通过 `requestAnimationFrame` 合并，同一输入框每帧最多执行一次布局读取和一次必要的高度写入，高度未变化时不得重复写 style。小手机外壳的 `MutationObserver` 只响应页面、底栏或主题等会影响 Home Indicator 的结构状态变化，必须忽略普通输入框自身的 style 高度变化，避免每次打字触发整屏底栏扫描。
_避免：每字符同步读写 `scrollHeight`、同一帧重复测量、textarea 高度变化触发 Home Indicator 全量刷新_

**叙事媒体消息**:
以纯文字承载内容、再由 QQ 前端渲染成语音气泡、图片卡片或视频卡片的消息，类型分别为 `voice`、`image` 和 `video`。语音正文是模拟说出的文字，显示时长按文字长度自动推导且不设上限；点击语音气泡会在其下方展开或收起原文，不播放声音或伪造播放进度。图片和视频正文是自然语言画面描述，直接显示在各自卡片中；视频没有真实封面、时长或播放行为。它们直接作为可读语义提供给 QQ AI 与世界书，不保存或引用真实音频、图片、视频，也不提供 URL 或媒体地址。
_避免：真实录音、系统选图、摄像头录像、媒体上传、多模态识别、把通用 Blob 资源能力误认为叙事媒体附件_

**类型消息组件**:
QQ 为叙事语音、叙事图片、叙事视频、转账和表情分别提供的一套类型化消息外观。每种类型在用户和 NPC 侧使用同一组件与同一语义，只按发送方向改变左右位置、颜色和头像；不为双方复制两套结构，也不把 NPC 的类型消息降级成普通文字。
_避免：用户和 NPC 两套类型组件、只有 NPC 使用美化消息、同一类型因发送方改变业务含义_

**转账消息**:
QQ 私聊中的结构化类型消息。金额和货币均允许自由文本，不建立真实余额或收支账本；用户确认转账表单后立即作为独立消息发送，走正常回复排队，不读取、不清空、不组合主文本草稿，发送后不能撤回。NPC 发给用户的待处理转账可点击打开“退还 / 收下”弹窗，选择后只更新为待处理、已收款、已拒收或已退还中的对应状态，不立即触发 QQ AI；下一次正常触发时，AI 才读取这次状态变化。
_避免：固定人民币、真实钱包、把用户转账并入或依赖主输入框提交、用户撤回转账、收下或退还后立即额外触发 AI_

**表情面板**:
聊天输入区工具栏下方展开的共享表情网格，与参考 QQ 中“输入框、工具栏、下方大面积表情区”的层级一致，不是输入框上方的小浮层。它作为覆盖层遮住聊天记录底部，而不缩短消息滚动区；基准 `402×874` 画面中参考高度为 `320px`、五列并在面板内部纵向滚动，窄屏为保证点击尺寸降为四列或三列。每格保持正方形，图片完整缩放而不裁切；首版不显示搜索、分类、GIF 或 AI 分类条。表情面板与系统软键盘互斥：打开面板时输入框失焦但保留文字，重新聚焦输入框时关闭面板；Esc 或手机返回先关闭面板。点击表情会立即作为独立消息发送，走正常回复排队，不读取、不清空、不组合主文本草稿；面板保持展开以便连续发送；再次点击表情按钮或点击面板外关闭。
_避免：输入区上方浮层、为面板重新计算消息区高度、同时显示软键盘和表情面板、裁切表情图片、伪造分类控件、让表情读取、清空或组合主文本草稿、每选一个表情都关闭面板_

**引用消息**:
一条消息通过被引用消息 ID 建立的回复关系；引用预览展示原消息，但不复制第二份原文。被引用消息硬删除后，关系仍可显示为“原消息已删除”，但不能恢复原文。
_避免：引用原文副本、删除后仍向 AI 泄露原文_

**QQ 人物**:
QQ 中代表单个 NPC 的稳定身份，可同时被一个私聊和多个群聊引用，并作为联系人页真实名单的数据来源；联系人页不复制第二份人物资料。解除好友关系后，只要该人物仍在群中，其身份就继续存在。
_避免：复制的联系人资料、复制的群成员资料_

同一 QQ 作用域内，规范化后的 NPC 名字用于人物去重；同名人物不会因手动创建或主动消息重复生成。通过消息页添加入口输入同名人物时，已有好友直接打开原私聊，非好友私聊仍在时恢复好友关系和原历史，私聊已硬删除但人物仍被其他数据引用时复用人物资料并建立新的空私聊，只有人物完全不存在时才新建身份；任何已硬删除历史都不会恢复。

人物拥有真实名字；私聊备注只影响私聊标题，群聊不建立额外群昵称并统一显示真实名字。

人物真实名字最多包含 32 个用户可见字符，按完整字素簇计数；保存前执行 Unicode 规范化、去除首尾空白并合并连续空格。对话页、联系人页和聊天标题中的名字保持单行省略并为两侧操作保留固定空间；人物资料页主名称最多显示两行后省略，编辑输入框保留完整值并允许内部滚动。任何槽位都不能为容纳长名字而动态缩小字号。

人物没有真实头像资源时使用姓名首字符头像：取名字的第一个可见字符，中文保留首个汉字，拉丁字母转为大写，数字保持原样；真实头像上传后覆盖占位头像。占位头像的明暗颜色、字体和尺寸必须来自小手机 UI 变量。

**QQ 好友关系**:
当前用户与一个 QQ 人物之间独立于私聊会话的好友关系。它决定人物是否出现在联系人页；删除或清空私聊会话不会解除好友关系。当前阶段只有用户能通过人物资料页中的“删除好友”动作解除该关系；解除时保留原私聊及历史、将其转为非好友私聊，并追加一条明确记录单方面解除行为方的系统消息。重新添加保留历史的非好友时，恢复原关系与会话并追加对应的关系恢复系统消息。两类事件始终保留通用行为方语义，以后允许 NPC 主动解除或恢复时不必改写旧记录。共同群聊中的成员关系和群历史不受影响。
_避免：把会话存在当作好友标记、删除聊天时顺带删除好友、当前阶段让 AI 触发删除好友、解除好友时级联退出群聊_

**私聊会话**:
用户与一个 QQ 人物之间的直接聊天单元，与 QQ 好友关系分别存在。好友可以暂时没有私聊会话；从人物资料页发送消息时可以重新建立空会话。私聊备注是该会话中的称呼：消息列表和私聊页标题优先显示备注，没有备注时回退人物真实名字；人物资料页以及未来恢复的群聊始终显示人物真实名字。删除或清空私聊只让会话从消息页消失，不会解除好友关系，也不会把人物从已有群聊中移除。
_避免：用私聊会话代替好友关系、删除私聊后级联踢出群聊_

**非好友私聊**:
QQ 好友关系解除后仍保留在消息页的原私聊会话。它完整保留历史但处于只读状态，用户不能继续发送，QQ AI 也不能在其中回复或主动发言；用户可以从该会话头像进入人物资料页并重新添加好友，恢复同一会话及其发送能力，不另建历史，同时追加真实的关系恢复系统消息。解除和恢复系统消息都属于该私聊历史，并明确记录行为方；恢复事件会让会话回到正常活动排序。用户若继续通过会话左滑删除确认移除它，则整段私聊及其历史被硬删除而非归档；没有其他数据引用的人物也随之清理。
_避免：删除好友时立即硬删历史、非好友仍可私聊、把已确认删除的会话暗中归档、用删除好友事件改写群成员关系_

**好友关系系统消息**:
QQ 好友关系解除或恢复时写入私聊历史的结构化系统事件，保存事件类型、行为方和关系双方身份。用户界面、AI 上下文与世界书统一使用双方真实名字渲染，不根据读者替换成“你、用户、对方”等相对称呼。
_避免：只保存写死文案、AI 材料中的模糊代词、不同消费方呈现不同关系事实_

**QQ 用户身份**:
QQ 中代表用户自己的身份，始终跟随 SillyTavern 当前用户角色的名字、头像和人物资料，不在 QQ 内建立或编辑第二份用户档案。
_避免：QQ 我的资料、独立 QQ 用户头像、QQ 用户档案_

**群聊**:
创建时从已有私聊好友中选择 QQ 人物组成的会话；它引用这些人物身份，同时拥有自己的群资料和消息。群头像始终由当前成员头像自动拼接，不能独立上传。非群主删除群聊表示退出，群主删除群聊表示解散整个群，均不影响任何私聊。
_避免：联系人集合、三人消息表、复制群成员资料_

**群聊停用**:
当前 QQ 前端融合阶段将群聊能力整体移出可用产品范围：不展示群聊入口、会话、页面或设置，也不运行群聊回复和群聊主动消息。群聊领域概念、既有实现和已有源数据不因此删除，留待未来重新接回；当前只继续提供私聊能力。
_避免：删除群聊实现、清空已有群数据、只隐藏界面却继续生成群消息_

**群身份**:
成员在单个群内拥有的权限角色，分为群主、管理员和普通成员。每个群只有一个群主，可以有多个管理员；只有群主和管理员能执行踢人、禁言等群管理动作。
_避免：所有群成员平权、全局人物权限_

**结构化提及**:
群消息中通过稳定人物 ID 指向当前群成员的 `@` 关系；只有从候选列表选择才会建立。直接手写的 `@名字` 是普通文本，不按名字猜测人物；提及被禁言成员也不会赋予其发言权。
_避免：按显示名字自动匹配、用提及绕过群权限_

**群管理动作**:
经过当前群身份校验后改变群名、成员、禁言或权限状态的操作。动作生效后生成群系统消息，但用户在群资料中执行动作本身不属于聊天发言，也不单独触发 QQ AI。
_避免：未校验的状态修改、把管理动作伪装成成员发言_

**已退出群聊**:
当前用户被群管理者踢出后保留的只读群会话；它继续展示已有历史，但不能发送新消息，也不会被 AI 自动硬删除，最终由用户决定是否从列表删除。
_避免：自动清空历史、仍可发言的普通群聊_

**已解散群聊**:
NPC 群主解散后保留的只读群会话；所有群活动终止，但已有历史由用户决定何时硬删除，AI 不能自动清除本地记录。
_避免：AI 自动删除历史、仍可继续管理的活跃群聊_

**QQ 底栏**:
QQ 的四个根级入口依次为“消息、联系人、助手、设置”。消息进入对话页，联系人进入联系人页，助手沿用 Figma 频道页的视觉外壳但保持空白，设置进入 QQ 全局设置。四个根级页都不显示页内返回按钮，设置一级页也不例外；切换栏位只替换当前激活页，不把经过的栏位加入返回历史，并让目标根页回到顶部。Figma 动态页不接入当前 QQ 或小手机，保留为未来小剧场广场的视觉候选。
_避免：照搬 Figma 的频道与动态业务、继续使用旧的两栏结构、设置根页返回按钮、返回键倒序穿过 Tab、跨 Tab 保留滚动位置_

**QQ 启动状态**:
用户从小手机主页面打开 QQ 时固定进入“消息”根页；上次所在根级 Tab、二级页面栈、列表与聊天滚动位置、未发送文字和临时交互层全部不恢复。
_避免：恢复上次 QQ 页面、跨启动保留输入草稿、重新打开时重现旧弹窗_

**QQ 页面栈**:
QQ 根级页之上的聊天、人物资料、编辑资料、会话详情和设置二级页所组成的来路感知导航历史。二级页通过页面上真实的返回控件回到上一个页面：从联系人进入人物资料再发消息，会从聊天返回人物资料再返回联系人；从聊天头像进入人物资料再编辑，则从编辑资料返回人物资料再返回原聊天。根级 Tab 切换不进入这个栈。没有临时交互层时，键盘 `Esc` 与系统后退在所有 QQ 页面均不执行导航，也不改写浏览器历史；返回小手机主页面只使用 Home Indicator。
_避免：二级页永远跳固定根页、返回直接清空全部 QQ 上下文、把根级 Tab 当成普通历史页、用 Esc 或系统后退暗中离开当前页_

**QQ 响应布局**:
以 Figma 的 `402×874` 画板作为视觉参考尺寸、由真实前端组件在小手机当前尺寸内流式重排的 QQ 页面。顶部背景、状态栏、根级底栏以及消息、联系人、助手、设置、资料和聊天页面都随可用内屏宽度铺满，保持单列但不把内容锁在 `402px` 居中窄栏，也不切换成双栏。Figma 的固定尺寸只定义基准控件、间距和圆角；宽度变化时由全宽容器、固定边距、弹性分栏、气泡宽度、网格列数或局部滚动共同适配，不把 Figma 画板或用户提供的参考截图作为页面图片嵌入，也不整体缩放文字与控件。实时缩放时，原本贴底的聊天继续贴底，正在阅读旧消息或列表时保持首个可见内容锚点。聊天正文按变量限制气泡最大宽度并纵向增长，不截断、折叠或横向滚动；长链接和连续无空格字符必须在气泡内安全断行。在 `200×400` 与可读文字 `160%` 的组合下，状态栏、页面返回、发送、根级底栏和 Home Indicator 等必要控制仍须可达；正文使用唯一主滚动区，弹窗正文可内部滚动且底部操作保持可达，不隐藏控制或缩小文字。
_避免：截图式页面、等比缩放整张画板、强制覆盖用户的小手机尺寸、非基准尺寸裁切内容、宽屏双栏、长消息撑宽聊天区、按内容缩小字号、极限尺寸下裁掉必要操作_

**QQ 转场**:
QQ 页面切换对运动方向的共同表达。根级 Tab 不使用方向滑动，只短暂淡入或直接切换；二级页前进向左、通过真实返回控件返回时向右；弹窗轻微淡入并缩放。全部时长和缓动来自小手机 UI 变量；系统要求减少动态时立即完成，不保留装饰性运动。
_避免：根级 Tab 冒充页面栈、QQ 自建不同于小手机的复杂动效、忽略减少动态偏好_

**QQ 内容读取状态**:
首次进入页面或切换 SillyTavern 聊天作用域时，QQ 立即显示当前页面的固定框架，尚未读完的数据内容区保持纯空白，不显示骨架、进度图标或“加载中”。读取失败时只显示一次短暂 toast，随后内容区继续留白，不保留错误文字或常驻重试入口。
_避免：把加载骨架误当成真实内容、全屏加载遮罩、在内容区常驻技术错误_

**QQ 临时交互层**:
添加菜单、内容输入弹窗、删除确认弹窗、表情面板和共享图片裁剪层等短暂界面，同一时刻只保留一个可操作层。模态弹窗会先关闭菜单和表情面板，表情面板会关闭菜单；确认步骤在同一视觉层内替换内容而不叠加第二层，并保留原表单数据以便取消后返回。只有最上层响应 Esc、返回或遮罩操作，模态打开时底层页面不可操作。模态打开后将键盘焦点限制在当前层内，关闭后优先恢复到原触发控件，原控件不存在时落到当前页标题或第一个可操作控件。用户一旦开始实时缩放小手机，所有临时交互层立即关闭，尚未提交的输入或选择直接丢弃。
_避免：弹窗套弹窗、多层遮罩、背景页面与顶层同时响应、进入确认后丢失表单、焦点逃到遮罩下方、缩放时保留旧弹层_

**对话页**:
QQ 底栏“消息”中的会话列表入口；群聊停用期间只展示私聊。视觉沿用 Figma 消息页；搜索框只作不可聚焦、不可点击的视觉元素，不提供搜索功能。右上角加号是真实的消息页添加入口。消息页当前用户标题下固定显示“在线 - WIFI”，私聊标题下固定显示绿色点和“在线”；两者都只是不可交互的视觉状态，不代表网络、API、生成进度或 NPC 真实在线。会话采用紧凑 QQ 列表；没有会话时仍保留顶部、搜索框和四栏底栏，列表区域完全空白，不显示文字、插图、引导按钮或淡化 QQ 标志。新消息使对应会话立即升到顶部；用户正在列表中部浏览时保持当前可见锚点，不自动滚回顶部。
_避免：消息记录表、把装饰性搜索框接入未定义的搜索业务、空列表占位内容_

**消息页添加入口**:
对话页右上角加号打开的真实两步添加流程。第一层沿用 Figma“消息页/添加弹窗”，群聊停用期间只显示“添加联系人”；点击后再打开姓名输入弹窗，用户输入人物名字并确认后建立 QQ 好友关系和私聊会话。姓名弹窗打开时自动聚焦；规范化后为空时确认按钮禁用，输入达到 32 个字素后停止接收更多字符，键盘回车等同确认；提交失败时保留弹窗和原输入，并在输入框下方显示错误，同名复用不算错误。成功后关闭两层弹窗并返回对话页，由用户再选择是否进入会话；添加本身不触发 AI 或自动生成第一条消息。两个弹层都使用 QQ 弹窗视觉，不能与联系人页右上角仅作装饰的添加图标混为一谈。
_避免：联系人页装饰按钮、直接跳过第一层菜单、停用期间显示建群或频道动作_

**联系人页**:
QQ 底栏“联系人”中的人物列表入口，沿用 Figma 联系人页视觉并列出当前仍有 QQ 好友关系的人物。“新朋友”“群通知”、搜索框和右上角添加联系人只作不可聚焦、不可点击的视觉元素，不提前建立好友申请、群通知、搜索或添加联系人业务。没有好友时固定视觉结构保持不变，人物内容区完全空白，不显示空状态说明。
_避免：为装饰入口伪造数据、把视觉占位误当成可用功能、空联系人提示_

**会话左滑删除**:
对话页中删除整个私聊会话的唯一首阶段手势。用户横向左滑超过阈值后只露出固定宽度的“删除”按钮，继续长滑也不会自动删除或自动打开确认；同一时间最多展开一行，向右滑回、点击其他会话或空白处、开始明显纵向滚动都会收起。不显示参考图中的“置顶”或“标为未读”，也不通过长按进入会话批量删除状态。只有点击删除按钮才使用与删除好友一致的 QQ 弹窗视觉进行二次确认；好友与非好友统一显示“确定删除该会话吗？删除后不可恢复”，按钮为“取消 / 删除”。

确认后会话进入删除中状态，阻止新发送、AI 写回和世界书同步，取消该会话的手动请求及当前整轮私聊主动周期；确认按钮只改为“删除中…”，不显示进度图标，弹窗按钮、遮罩关闭和返回暂时锁定。QQ 必须先清除当前及历史目标世界书中的自有投影，再一次性硬删除私聊历史、会话设置和背景资源，但不会解除仍然存在的 QQ 好友关系。任何清理失败都保留完整会话并恢复弹窗操作，在弹窗内只显示“删除失败，请重试”；成功后关闭弹窗、移除会话行且不显示额外成功提示。
_避免：长按会话批量选择、首阶段实现置顶或标为未读、删除会话时顺带删除好友、乐观隐藏后失败回插、残留世界书投影_

**人物资料页**:
Figma 中名为“用户页”的 QQ 人物资料页面，从联系人条目或聊天页人物头像进入。好友状态下底部提供“删除好友、编辑资料、发消息”；非好友状态下改为“添加好友、编辑资料、查看消息”。添加好友恢复原 QQ 好友关系和同一私聊，编辑资料进入对应二级页，发消息打开现有私聊或建立新的空会话，查看消息回到保留的只读私聊，删除好友先显示确认弹窗。该名称既适用于好友也适用于非好友，并与始终跟随 SillyTavern 的 QQ 用户身份区分。
_避免：只适用于好友的资料页名称、QQ 用户自己的资料页、把 NPC 资料复制为第二份身份_

**私聊页**:
QQ 人物之间直接聊天的页面。标题使用私聊备注并以人物真实名字兜底；点击 NPC 头像进入人物资料页，点击右上角菜单进入会话详情。进入时显示最近的消息渲染窗口并定位到底部；用户停留在底部附近时跟随新消息，阅读旧消息时保持当前位置，并使用 Figma 的数字小气泡显示这段时间新到达的消息数量，点击气泡跳到最新消息。移动端软键盘出现时，输入区始终位于键盘上方，消息可视区相应缩短；原本贴底则继续贴底，阅读旧消息时保持可见锚点，跳底气泡移到输入区上方。软键盘不会覆盖输入区，也不会整体平移或缩放小手机壳。
_避免：用右上角菜单代替人物头像入口、阅读旧消息时强制滚到底部、把跳底气泡计入会话未读数、软键盘遮住输入区、为避让键盘移动整部手机_

**会话摘要**:
对话页用最后一条消息生成的单行预览。文字显示内容，语音、图片、视频、表情和转账使用 QQ 式方括号标签；群聊成员消息带人物名前缀。存在未回复批次时，摘要额外显示红色感叹号和“发送失败”。有消息的会话右侧只用故事时间生成 Figma 式相对时间：同一故事日期显示 `HH:mm`，前一日显示“昨天”，2–6 日显示“N天前”，7–27 日显示“N周前”，更早显示故事日期；故事时间缺失或无法解析时留空，不能回退现实时间。全新空私聊只显示头像和人物名，摘要、时间和未读数全部留空，不显示“暂无消息”或伪造系统消息，并统一排在所有已有真实消息的会话之后；多个空会话之间沿用存储自然顺序，不增加稳定排序字段。
_避免：媒体内部描述直接泄露、跨会话拼接摘要、失败状态只藏在聊天页、浏览器时间、为空会话伪造活动顺序_

**消息渲染窗口**:
聊天页为了界面性能从 IndexedDB 分批显示的消息范围，首次为最近 50 条并直接定位到最新消息底部；接近顶部时自动再读取 50 条，并保持当前可见消息的滚动锚点。读取到最早消息后不显示“没有更多”之类的提示。它不改变消息真实存储，也不能限制 AI 历史、世界书同步、全选删除或其他业务读取。
_避免：聊天记录分页数据、只保存最近 50 条、用 DOM 作为历史来源_

**未读数**:
未打开会话收到 NPC 消息后累计的待查看数量。打开该会话时立即清零；当前会话只要处于打开状态，新消息就不累计未读。离开聊天页时立即关闭该会话的已打开状态，但不取消已经发起的 AI 请求；离开后到达的 NPC 消息正常计入未读并使会话升到列表顶部。会话头像显示本会话数字，底栏“消息”和小手机桌面 QQ 图标显示总数，超过 99 均显示 `99+`。硬删除会话成功时立即从全部总数中移除该会话的未读数量，但不生成已读事件。
_避免：要求滚到底部才清零、浏览器通知数量、离开聊天页后继续视为打开、返回列表就取消 AI、已删除会话留下假角标_

**助手页**:
QQ 底栏“助手”中为以后功能保留的空白入口；它沿用 Figma 频道页的视觉外壳，但 QQ v2 不在这里承载频道、会话、表情、设置或其他功能。
_避免：真实频道页、QQ 设置页、AI 聊天页_

**QQ 全局设置**:
从 QQ 底栏“设置”进入的运行设置入口，并复用 Figma“用户页 / 编辑资料”的一级页与二级页结构作为统一视觉。页面内的 API 与各类 AI 指令预设选择、主动消息总开关与触发间隔、宿主上下文条数与 QQ 会话历史条数，以及世界书注入总闸、时间跨度、全局灯色、全局深度和全局关键词，全部属于扩展级全局设置，切换 SillyTavern 聊天后保持不变。世界书注入目标 `bookName` 不属于这些全局默认：它按当前 SillyTavern 宿主聊天保存；该宿主聊天没有手动选择时，默认解析当前角色卡绑定的主世界书，没有主世界书则使用第一个附加世界书；手动选择具体世界书只覆盖当前宿主聊天，切换到其他宿主聊天后仍使用其自身选择或默认角色卡解析结果。主动周期的已累计轮数 `count` 和下一轮类型 `nextKind` 也按宿主聊天独立保存。每个选项修改后立即保存，不存在整页保存按钮；QQ 单会话详情中的世界书注入开关、灯色与深度覆盖、关键词等仍按各 QQ 会话独立保存，可继续跟随全局默认或使用本地覆盖。 新增的标签提取与标签排除也属于扩展级全局运行设置：标签名持久化为不带尖括号的规范名称，默认提取标签为 `content`，排除标签支持多个名称；`{{正文上下文}}` 先按宿主上下文条数选取成功的 AI 楼层，再提取配置标签内容、移除排除标签块，找不到提取标签时保留原正文，不再添加无意义的“角色：”前缀。
_避免：会话设置、预设编辑器_

**会话详情**:
从聊天页右上角进入的单会话设置页。当前私聊会话详情只管理该会话的备注、聊天背景、世界书注入开关，以及灯色、深度、关键词等世界书注入项的全局跟随或本地覆盖状态；这些配置按 QQ 会话独立保存。页面同时提供进入人物资料页的入口；人物真实名字和头像属于人物资料页，不在这里重复编辑。群聊停用期间不展示群会话详情。
_避免：QQ 全局设置、把会话详情当成人物资料页、在会话详情重复编辑人物身份_

**聊天背景**:
单个私聊可选择的本地图片，只覆盖消息滚动区；头部、输入区和工具栏继续使用当前主题皮肤。上传时保留原始图片 Blob，显示时相对聊天视口固定并居中 `cover`，窗口缩放时按比例从中心裁切，不随消息滚动、不拉伸也不平铺；QQ 不为聊天背景打开裁剪弹窗。
_避免：覆盖整个私聊页、随消息内容滚动、上传时改写原图、重复实现 QQ 图片弹窗_

**表情库**:
QQ 中保存真实图片表情及其 AI 可理解说明的全扩展共享集合；用户、所有私聊 NPC 和所有群成员都能使用其中任意表情。它不随 SillyTavern 正文聊天切换，也不存在人物或会话专属表情。聊天表情面板的加号与图片资料中的表情仓库复用同一批量录入流程：按系统文件选择顺序展示，每张图片独立填写说明，整批校验并原子保存。表情显示使用 `contain` 保留完整图片。从库中删除图片后，历史表情消息不显示“已经失效”之类的占位文案，只保留发送时的文字说明，继续作为 QQ AI 与世界书可读的历史内容。
_避免：我的表情、人物表情、会话专属表情、批量顺序漂移、部分批次落库、裁切表情图片、删除图片后破坏历史文字语义_

**QQ 本地图片资源**:
QQ 为当前用户与 NPC 头像、资料背景、会话背景和表情图片保存的浏览器本地资源。图片内容以原始 Blob 存入 IndexedDB 资源表，人物、会话和消息只引用资源 ID；单个文件上限为 `8MB`，上传过程不裁剪、不压缩、不转为 base64。头像和背景由显示容器居中 `cover`，表情由显示容器 `contain`。已保存资源读取失败时不显示浏览器坏图或重试入口：头像回退姓名首字符，聊天背景回退当前主题纯色，表情消息回退发送时保存的文字说明；资源引用仍保留，后续渲染可以再次读取。只有用户刚执行的上传或保存失败才显示一次 toast，之后由用户自行重新上传。
_避免：base64 塞入业务记录、上传时改写原图、外部图片 URL 作为主存储、每处复制一份图片、坏图占位、常驻重试按钮、静默清除资源引用_

## AI 配置

**小手机设置**:
创建和维护 QQ 可复用 API 预设、AI 指令预设与世界书读取范围的系统设置入口，不承载 QQ 日常运行选择。首页提供“API 预设”、“AI 指令预设”和“读取世界书”三个独立入口；数据库填表预设、剧情推进预设以及无消费方的旧世界书工作台仍保持删除。“数据库当前 API”只作为 QQ 运行时可识别的只读虚拟预设出现，不成为首页快捷配置或普通预设存储。
_避免：QQ 全局设置、复制数据库连接配置、首页数据库预设快捷选择、恢复旧世界书工作台_

**读取世界书栏**:
小手机设置中管理 `{{世界书内容}}` 候选条目的一级页。它只列出当前角色绑定的主世界书与全部附加世界书，默认读取其中所有未禁用条目；禁用条目不可选，用户取消的条目以 `{ 书名: { UID: false } }` 稀疏覆盖保存。它没有总开关，也不提供手工添加世界书的模式；世界书更新时自动刷新条目。
_避免：QQ 世界书注入开关、只选一本书、默认关闭条目、选中禁用条目、写入 `true` 全量配置_

**API 预设**:
普通 API 预设是用户为 QQ 保存的一组扩展级共享 AI 接口连接与生成参数；各 SillyTavern 聊天作用域只引用其稳定 ID，不复制密钥或预设正文。数据库插件可用时，QQ 共享资源额外提供一个名为“数据库当前 API”的只读虚拟预设；它只携带稳定虚拟 ID 与显示名，QQ 请求通过数据库官方受限接口 `window.AutoCardUpdaterAPI.callAI(messages)` 使用数据库当前配置，不读取或复制 URL、密钥、模型、温度等配置，不进入普通 API 预设存储。小手机 API 预设管理页禁用该项的编辑、保存、删除和加载模型；QQ 的运行时 API 选择器仍可选择并调用它。数据库插件不可用时不展示该虚拟预设，已选中的请求保持 fail-closed；本规则不引入“本地 API 不填密钥”模式。
_避免：读取或复制数据库当前配置、把虚拟预设持久化为普通预设、恢复旧数据库 API 管理接口、数据库不可用时静默回退到其他 API_

**AI 指令预设**:
用户可查看和编辑的扩展级共享 QQ AI 指令预设库。预设自身没有私聊、群聊、主动或回复的类型字段；初始提供四份玉子式内置预设，QQ 全局设置再为私聊回复、私聊主动消息、群聊回复和群聊主动消息分别引用要使用的预设 ID。小手机管理页通过一个统一下拉框切换全部内置和自定义预设；六个管理操作始终可见并在窄屏自动换行。
_避免：隐藏提示词、内置死提示词_

**消息块**:
AI 指令预设中的一条可编辑消息，具有 `system`、`user` 或 `assistant` 角色，并在预设中保持明确顺序。
_避免：提示词模块、规则模块_

**输出格式消息块**:
每份 AI 指令预设末尾用于告诉模型如何输出 QQ XML 的普通消息块；它完全开放编辑，不作为隐藏系统指令，运行时只使用固定 XML 解析器。
_避免：锁定协议、隐藏 XML 指令_

**占位符**:
写在消息块内容中的运行时标记；QQ 提供私聊人物、私聊主动人物、群聊成员、私聊主动记录、群聊记录、正文上下文、世界书内容、故事时间和可用表情等占位符。其中私聊人物表示人物名字，私聊主动人物表示联系人所有人名字；私聊回复历史由当前 QQ 会话以真实角色消息自动追加。
_避免：动态变量、提示词模块_

**正文上下文**:
**QQ 正文上下文**:
QQ AI 从当前 SillyTavern 正文读取的最近若干条成功完成的 AI 楼层正文；宿主上下文条数按成功角色回复计数。当前实现不包含用户输入、系统消息、隐藏推理或页面渲染文本。标签提取与标签排除在这些 AI 楼层正文上生效：先提取配置标签，再移除排除标签块；未找到提取标签时保留原正文。
_避免：正文消息条数、渲染后 DOM、全部系统提示词_

**QQ 历史范围**:
本次 QQ AI 从当前会话读取的最近若干条独立消息气泡；各种消息类型和群系统消息各算一条，`0` 表示读取全部当前会话历史。
_避免：正文轮数、跨会话聊天记录_

**主动消息**:
用户在 QQ 主设置中开启默认关闭的主动消息总开关后，正文累计经过用户设定的轮数，由 QQ 自动发起 AI 请求所产生的消息；私聊与群聊使用各自的主动消息预设，不存在人物或群聊级候选开关。
_避免：概率检查、人物级主动开关、群聊级候选开关_

**主动周期**:
主动消息总开关开启后，正文每累计 N 轮触发的一次 QQ 主动请求，N 默认 `5`。开启、重新开启或修改 N 都由零计数并从私聊周期开始；关闭会取消正在生成的主动请求。主动周期在私聊和群聊之间固定轮换，每周期只调用对应的一套主动消息预设一次；私聊周期读取全部现有私聊，群聊周期读取全部正常群聊，并把当前用户被群管理者踢出、尚可合法重新邀请的只读群作为仅重邀候选。一次周期可以让零个、一个或多个会话行动，也可以创建新私聊或新群聊。内部进度不显示在界面。群聊停用期间只运行私聊周期；任一私聊确认硬删除时，当前整轮私聊主动周期一并取消且不落地，下一次周期再按最新会话状态重新开始。一次主动周期没有新增、删除或修改任何可投影 QQ 事实时，真实世界书保持不变。
_避免：每个会话各调一次、先用另一个 AI 判断、把多会话伪装成一段角色历史_

**未回复批次**:
一个会话从上一次成功 QQ AI 回复之后开始、尚未获得成功回复的全部用户消息。它们仍是真实 QQ 消息，保留在原位置并正常参与世界书同步；用户可以用最新设置重试，也可以通过统一消息选择模式删除。请求失败时只在该批最后一条用户消息下显示一个可点击的红色旋转图标，不显示“重新发送”文字或内部错误；点击图标会用最新设置重试整个批次，前面的相关用户消息不重复显示失败标记。
_避免：失败草稿、已撤回输入、失败 AI 消息、每条消息各自重试、列表中直接触发重试_

**手动请求队列项**:
一个会话等待 QQ AI 回复的唯一排队位置。同一会话继续发送时只更新该项所代表的最新未回复批次，不重复排队也不改变原有先后顺序；真正执行时才读取最新批次与运行设置。
_避免：同一会话重复排队、排队时冻结消息副本_

**已读不回**:
私聊 NPC 明确读完当前未回复批次但不发送新消息的处理结果。它关闭该批次，并只在最后一条相关用户消息旁显示“已读”；私聊手动回复和私聊主动消息都可以产生该结果。
_避免：伪造空白 NPC 消息、继续把已读内容当作未回复重发_

**AI 动作批次**:
一次 QQ AI 返回中按顺序提出并统一校验的一组消息或状态动作。后续动作可以依赖同批前序动作形成的草稿状态，但只有整批全部合法才会一次性生效。
_避免：部分保存、跳过非法动作、边解析边落库_

**世界书注入**:
把 QQ 会话消息同步为 SillyTavern 中真实世界书条目的过程；它是 QQ 数据的对外投影，不是消息的保存来源。每个会话在每本受 QQ 管理的目标世界书中最多一条新版投影，名称为 `YuziQQ｜私聊｜人物真名｜<完整 conversationId>` 或 `YuziQQ｜群聊｜群名｜<完整 conversationId>`；完整 `conversationId` 是投影的稳定主身份，人物或群聊改名只更新同一条目。持久化的 `entryUid` 用于快速定位，条目中的 QQ 扩展标记只提供辅助识别，标记缺失不会产生第二条投影。新版投影正文整体以独立首行 `<yuzi>` 和独立末行 `</yuzi>` 包裹；正文先说明会话，再按故事日期分段，用当前酒馆用户角色名、NPC 真实名字和完整自然语言媒体描述写入。每条具有合法故事时分的消息以 `[HH:mm] 发言者：原始内容` 输出；只有日期或故事时间未知、无效的消息不得伪造 `[00:00]`，只保留日期分段或未知故事时间分段与无时分的消息正文。群聊开头只列当前成员，不额外写权限。已有新版投影在下一次正常同步时按原 UID 刷新为该正文格式，不另建迁移条目。更换目标世界书是移动而非保留历史副本：先把新版受管投影写入新目标，再删除旧目标中的对应新版受管投影；任一步失败都会尝试回滚，若无法完全恢复则进入待同步。关闭全局或单会话注入、硬删除 QQ 会话或删除 SillyTavern 宿主聊天时，QQ 清理当前及历史目标书中的对应新版投影。旧 `QQ｜私聊｜...`、`QQ｜群聊｜...` 条目属于用户自行处理的遗留数据，即使仍带旧扩展标记也不改名、不更新、不禁用、不删除、不提示，也不会补写 `<yuzi>` 标签或消息时分。
_避免：伪造正文提示词、临时上下文注入_

**世界书待同步**:
QQ 数据已经成功保存，但对应世界书投影尚未可靠收敛的可恢复状态。世界书读取或保存失败时，系统在后续消息、注入设置变化或重新进入会话时自动重试，成功后清除；同一本书中出现多条带有同一完整 `conversationId` 的新版投影时则停止更新且绝不再创建下一条，保持待同步直至用户手工清理到只剩一条。待同步不能回滚或隐藏真实 QQ 消息，也不会自动删除、禁用或任选一条重复投影。
_避免：撤销 QQ 消息、把错误文本写进聊天或世界书、永久静默放弃_

**QQ 上下文读取**:
QQ AI 生成前读取正文和世界书资料的过程，与把 QQ 消息写入真实世界书的“世界书注入”是两条不同链路。
_避免：世界书注入、QQ 消息投影_

QQ 的世界书上下文不再分“剧情层 / 人物层”，也不依赖正文世界书快照。每次手动回复以最近两条有效正文消息、当前 QQ 人物真实名字和当前会话最近 3 条未删除消息组成扫描文本；每次主动消息则使用本周期全部相关人物真实名字，并让每个相关会话独立贡献最近 3 条未删除消息。语音、图片、视频、表情、转账和关系系统消息都先转成与 QQ AI 一致的可读语义。

扫描只面向“读取世界书栏”选中的未禁用条目：蓝灯条目全部激活，绿灯条目按主键与可选辅助键逻辑激活，条目内容可继续递归激活其他条目，最终按书名与 UID 去重，并排除 QQ 自己写入的聊天投影。这条读取链不执行 SillyTavern 的 token 预算、概率、包含组竞争与权重、delay/cooldown、角色/标签过滤或 generation trigger。条件 EJS 与 MVU 数据参与激活前的条目增删和键预处理，激活后先逐条完成 EJS/MVU 渲染，再把全部条目合并为一次请求级 shujuku SQL/模板解释会话，使 `$v` 与 random/calc/max/min 定义可跨条目引用，最终结果填入 `{{世界书内容}}`。世界书 API 整体读取失败时静默返回空内容，不阻断 QQ 生成。
_避免：正文世界书快照、调用完整酒馆预设、读回 QQ 投影、用 token 预算截断蓝灯条目、一个会话的历史额度挤掉另一个会话_

**故事时间**:
全局数据表中描述剧情当前时刻的值，是 QQ 消息时间窗口的唯一时间依据。
_避免：浏览器时间、保存时间_

当世界书注入条数为 `0` 时，故事时间缺失或无法解析仍不会选择自动消息，只保留用户手选内容；当世界书注入条数大于 `0` 时，为保证投影不会因故事时间暂时不可用而清空，自动选择降级为当前会话按消息顺序最新的指定条数。两种情况都不能使用浏览器现实时间伪造故事时间。

私聊页首次渲染窗口的最早一条消息前显示居中时间分隔；之后在相邻消息跨故事日期或故事时间相差至少 30 分钟时再次显示。分隔相对当前故事日期显示“今天 HH:mm”“昨天 HH:mm”或完整故事日期与时间；故事时间缺失或无法解析时不显示，也不使用浏览器现实时间补造消息时间。
